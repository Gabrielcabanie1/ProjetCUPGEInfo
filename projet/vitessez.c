#include <stdlib.h>
#include <stdio.h>
//#include <main.h>
#include <ctype.h>

int vitesse_z(int (*point).x,int (*point.y),int (*point.z),B)
{
    int res= ((point->x) * (point->y)-(point->z) * B);
    return(res);
}